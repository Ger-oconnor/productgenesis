# Editor / OS
.obsidian/
.superpowers/
.DS_Store
Thumbs.db

# Claude local settings (API keys etc.)
.claude/settings.local.json

# Uploads (binary assets — add manually if needed)
uploads/

# Analytics dashboard — contains live GA traffic data, regenerated locally by Claude
analytics.html

# Google Analytics credentials — never commit
ga-service-account.json
ga_token.json
